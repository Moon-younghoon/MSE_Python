data="039490   "
data1=data.rstrip()
print(data1)
#40번에서는 strip method를 이용하여 양쪽의 공백을 제거할 수 있었다.
# 오른쪽에만 공백이 존재하는 경우에는 list method인 rstrip method를 사용해 준 후 새롭게 반환된 문자열인 data1을 실행시킨다.
#출력된 data1값은 "039490"일 것이다.
# 왼쪽 공백을 지우고 싶다면 위 방법과 같은lsrtip method를 사용해 주면 된다.