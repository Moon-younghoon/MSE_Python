import numpy
for i in numpy.arange(0, 5, 0.1):
    print(i)

#import를 이용해 numpy모듈을 불러온다.
#for문과 i 를 이용해 numpy 에서의 변수를 지정해주고 arrange 함수를 이용해 0부터 5까지 0.1씩 늘어나도록 한다.(for i in numpy.arrange(0,5,0.1) 
#i를 print를 이용해 출력해준다.